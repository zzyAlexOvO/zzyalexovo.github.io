package ballboy.Entities;

import java.util.ArrayList;

public class LandScape extends NormalEntity{
    public LandScape(double x, double y, double height, double width, Layer layer, String images) {
        super(x, y, height, width, layer, images);
    }
}
