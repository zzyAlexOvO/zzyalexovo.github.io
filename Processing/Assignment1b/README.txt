To run the program, simply use processing to run the "SID470482647_Assignment1b.pde" file.

Then to throw a ball, simply click the mouse in the window, a ball of random texture would be threw from where mouse clicked.